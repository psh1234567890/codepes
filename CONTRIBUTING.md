# CodePes에 기여하기

CodePes에 관심을 가져 주셔서 감사합니다. 작은 오탈자 수정부터 데이터 검증, 접근성, 테스트, 기능 개선까지 모두 의미 있는 기여입니다.

## 시작하기

1. 저장소를 fork하고 작업 브랜치를 만듭니다.
2. Node.js 20 이상에서 의존성을 설치합니다.
3. 변경 범위를 작게 유지하고, 관련 테스트와 문서를 함께 수정합니다.

```bash
npm ci
npm run dev
```

Windows PowerShell에서 `npm.ps1` 실행이 차단되면 `npm.cmd`를 사용하세요.

## 변경 전 확인

- 같은 내용의 이슈나 Pull Request가 이미 있는지 검색합니다.
- 큰 기능이나 데이터 모델 변경은 구현 전에 이슈에서 방향을 논의합니다.
- 보안 취약점은 공개 이슈에 쓰지 말고 [보안 정책](SECURITY.md)을 따릅니다.
- 모든 참여는 [행동 강령](CODE_OF_CONDUCT.md)을 따릅니다.

## 코드 기여

- 기존 TypeScript와 React 구조를 유지합니다.
- 브라우저 전용 기능은 서버나 계정이 있는 것처럼 오해하게 만들지 않습니다.
- 접근 가능한 이름, 키보드 조작, 명확한 포커스 상태를 유지합니다.
- 외부 데이터는 사용 전에 런타임 검증을 거쳐야 합니다.
- 새 의존성은 꼭 필요한 경우에만 추가하고 라이선스를 확인합니다.

제출 전 전체 검증을 실행하세요.

```bash
npm run verify
```

개별 명령은 다음과 같습니다.

```bash
npm run typecheck
npm run test
npm run build
```

## 대회 데이터 기여

대회 데이터는 사용자의 실제 일정에 영향을 주므로 코드 변경과 같은 수준으로 검토합니다.

1. 주최 기관이 운영하는 공식 URL을 준비합니다.
2. 참가 신청 마감, 행사 시작·종료, 참가 자격, 진행 방식을 원문과 대조합니다.
3. 자동 수집 출처가 아니라면 `data/manual-contests.json`에 항목을 추가하거나 수정합니다.
4. `lastVerifiedAt`에는 실제로 공식 페이지를 확인한 시각을 ISO 8601 형식으로 기록합니다.
5. `npm run sync`와 `npm run verify`를 실행합니다.

블로그, 커뮤니티 재게시물, 검색 결과 요약만으로는 등록하지 않습니다. 공식 페이지가 변경되었거나 사라졌다면 Pull Request 설명에 그 사실을 명시하세요.

Codeforces API 항목은 별도의 신청 마감 정보가 없기 때문에 `applicationDeadline`에 대회 시작 시각을 사용하고 `deadlineKind`를 `start`로 설정합니다.

## Pull Request

Pull Request에는 다음 내용을 포함해 주세요.

- 무엇을, 왜 바꾸었는지
- 사용자에게 보이는 변화
- 실행한 검증 명령과 결과
- UI 변경이 있다면 데스크톱·모바일 화면 또는 재현 방법
- 데이터 변경이라면 공식 출처 URL과 확인 시각

한 Pull Request에는 가능한 한 하나의 목적만 담아 주세요. 리뷰 중 요청된 변경은 새 커밋으로 추가해도 되며, 병합 방식은 유지관리자가 저장소 상황에 맞게 결정합니다.

## 문서 기여

문서는 한국어 독자가 한 번에 이해할 수 있는 짧고 구체적인 문장을 우선합니다. 현재 구현되지 않은 기능은 완료된 것처럼 쓰지 말고 “계획” 또는 “제안”으로 구분합니다.

## 라이선스

기여물을 제출하면 해당 기여물을 저장소의 [MIT License](LICENSE)에 따라 배포하는 데 동의하는 것으로 간주합니다. 제3자의 코드, 이미지, 데이터는 재배포가 허용되는지 확인하고 출처와 라이선스를 Pull Request에 밝혀 주세요.
